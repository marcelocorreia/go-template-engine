---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/terraform
    tag: 'latest'

inputs:
- name: git-repo-master

outputs:
- name: package
- name: ansible

params:
  AWS_ACCESS_KEY_ID:
  AWS_SECRET_ACCESS_KEY:
  MAKE_TARGET:
  SSH_KEY:
  CLUSTER:
  SSH_USER:
  INVENTORY_PATH:

run:
  path: git-repo-master/gallifrey/scripts/make.sh

