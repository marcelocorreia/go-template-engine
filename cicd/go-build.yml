---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: {{job_input}}

outputs:
- name: {{job_output}}

run:
  path: go-repo-dev/make.sh
  args: {{make_args}}