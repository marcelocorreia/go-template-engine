---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: go-repo-dev

outputs:
- name: {{job_output}}

run:
  path: go-repo-dev/make.sh
  args: {{make_args}}