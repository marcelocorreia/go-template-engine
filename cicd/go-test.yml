---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: go-repo-dev

outputs:
- name: package

run:
  path: go-repo-dev/make.sh
  args: [test]