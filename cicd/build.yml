---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: go-repo

outputs:
- name: dist

run:
  path: go-repo/make.sh
  args: [package]