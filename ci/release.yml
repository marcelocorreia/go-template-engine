---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: git-repo-master

outputs:
- name: package

params:
  MAKE_TARGETS:

run:
  path: git-repo-master/make.sh