---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: git-repo-master

outputs:
- name: output

params:
  MAKE_TARGETS:
  VERSION:

run:
  path: ls
  args: -l