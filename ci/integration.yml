---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: marcelocorreia/go-glide-builder
    tag: 'latest'

inputs:
- name: git-repo-dev

outputs:
- name: package

params:
  MAKE_TARGETS:

run:
  path: git-repo-dev/make.sh